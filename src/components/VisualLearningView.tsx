import React from 'react';
import TrafficLessons from './TrafficLessons';

export default function VisualLearningView() {
  return (
    <div id="visual-learning-view" className="py-2">
      <TrafficLessons standalone />
    </div>
  );
}
